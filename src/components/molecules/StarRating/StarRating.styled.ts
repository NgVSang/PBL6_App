import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignSelf: "flex-start",
    gap: 10,
  },
  starImage: {
    width: 40,
    height: 40,
    resizeMode: "contain",
  },
});
